spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sustc  # change this to your own jdbc url
    username: bilibili  # change this to your own username (avoid using a superuser)
    password: 123456  # change this to your own password

    hikari:
      connection-timeout: 30000
      max-lifetime: 180000
      max-pool-size: 16
      leak-detection-threshold: 120000

logging:
  level:
    root: off
    io.sustc: debug

---

benchmark:
  data-path: data/small_data
  student-mode: true
